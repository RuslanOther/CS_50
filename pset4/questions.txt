0.  BMP - "Microsoft предлагает битности 1, 2, 4, 8, 16, 24, 32, 48 и 64" (Wiki)
    JPG - 24 бита 
    PNG - 16, 24 32 бита
    GIF - 8 бит

1.  Gif

2.  Сжатие без потерь подразумевает полное восстановление данных после декомпрессии. 
    С потерями - при декомпрессии файл будет отличаться от исходника.

3.  JPG

4.  Первый знак имени в таблице заменяется спецкодом. Т.е., грубо говоря, затирается информация о файле в таблице, 
    но сам файл остается нетронутым.
    
5.  Используется многократная запись нулей в место, где ранее был файл. 

6.  Заголовочный файл, который описывает целочисленные типы данных

7.  Это типы с заданной шириной. Которые позволяют 

8.  BYTE: 1 bytes
    DWORD: 4 bytes 
    WORD: 2 bytes
    LONG: 4 bytes

9.  BM

10. В bfSize хранится размер файла рисунка, в байтах.
    В biSize - размер структуры в байтах и должен быть равен 40 байтам.
    
11. Если biHeight - отрицательное значение, 
    точечный рисунок - нисходящий аппаратно-независимый растровый формат (DIB), 
    а его начало координат - левый верхний угол.

12. biBitCount

13. Например если винт не доступен, есть ошибка в системе. 
    Т.е. когда нет возможности создать файл. 

14. Чтобы прочитать только один объект из потока.

15. 3

16. Эта функция используется для произвольного перемещения по файлу.

17. Устанавливает отправную точку функции fseek() - от текущей позиции

18. Без понятия.
